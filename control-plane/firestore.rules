rules_version = '2';
service cloud.firestore {

  // TODO - add authentication so only authenticated users can read/write

  match /dabases/{database}/documents {
    match /{document=**} {
      allow read: if true;
    }
  }
}
